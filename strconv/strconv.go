package strconv

func String(v any) string {
	// If implements stringable, call String()

	// If is basic, use strconv

	// If is slice or array, use slices package

	// If is map, use maps package

	// Else use fmt.Sprintf

	return ""
}
